import React from 'react';
import {Typography} from 'antd';

const {Title} = Typography;

const CreateTripTitle = () => {
  return (
    <Title className='create-trip' level={2}>Create your Trip</Title>
  );
};

export default CreateTripTitle;
